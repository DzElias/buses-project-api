{
  "name": "api-buses-server-and-client",
  "type": "module",
  "main": "server/index.js",
  "engines": {
    "node": "16.15.0",
    "npm": "8.5.5"
  },
  "scripts": {
    "start": "npm install && node server/index.js",
    "dev": "nodemon server/index.js",
    "build": "npm --prefix client install && npm run --prefix client build"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.3.1",
    "morgan": "^1.10.0",
    "socket.io": "^4.5.1",
    "body-parser": "^1.20.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
